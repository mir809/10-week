extends templates/layout

block content
  h1=pageTitle
  form(method="post")
    input(name="title" placeholder="Title" required maxlength=50)
    input(name="synopsis" placeholder="Synopsis" required minlength=10)
    input(name="genres" placeholder="Genres" required)
    input(type="submit", value="Add Movie")

  
    
